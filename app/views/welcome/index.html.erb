<div class="lp">

  <!-- ════════ HEADER ════════ -->
  <header class="lp-header">
    <div class="lp-container lp-header__inner">
      <a href="/" class="lp-header__logo">
        <%= image_tag 'readingpro_logo.png', alt: 'ReadingPRO', style: 'height: 48px; width: auto;' %>
      </a>
      <nav class="lp-header__nav">
        <a href="#features" class="lp-header__link">주요 기능</a>
        <a href="#audience" class="lp-header__link">대상별 안내</a>
        <a href="#process" class="lp-header__link">도입 절차</a>
      </nav>
      <a href="<%= login_path %>" class="lp-btn lp-btn--primary lp-btn--sm">로그인</a>
    </div>
  </header>

  <!-- ════════ ANNOUNCEMENTS ════════ -->
  <% if @announcements.any? %>
    <div class="lp-announce">
      <div class="lp-container">
        <% @announcements.each do |a| %>
          <div class="lp-announce__item">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
            <span><%= a.content %></span>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>

  <!-- ════════ HERO ════════ -->
  <section class="lp-hero">
    <div class="lp-hero__bg">
      <div class="lp-hero__orb lp-hero__orb--1"></div>
      <div class="lp-hero__orb lp-hero__orb--2"></div>
      <div class="lp-hero__orb lp-hero__orb--3"></div>
    </div>
    <div class="lp-container lp-hero__inner">
      <div class="lp-hero__copy">
        <span class="lp-hero__badge">AI 기반 읽기 교육 플랫폼</span>
        <h1 class="lp-hero__title">읽기의 힘,<br><em>진단에서 성장까지</em></h1>
        <p class="lp-hero__subtitle">
          전문 연구진이 개발한 평가 문항과 AI 자동 채점으로<br>
          학생별 읽기 역량을 정확히 진단하고, 맞춤 성장을 지원합니다.
        </p>
        <div class="lp-hero__cta">
          <a href="<%= login_path %>" class="lp-btn lp-btn--white lp-btn--lg">시작하기</a>
          <a href="#audience" class="lp-btn lp-btn--ghost-white lp-btn--lg">자세히 보기</a>
        </div>
        <div class="lp-hero__stats">
          <div class="lp-hero__stat">
            <span class="lp-hero__stat-num">10분</span>
            <span class="lp-hero__stat-label">진단 완료</span>
          </div>
          <div class="lp-hero__stat-divider"></div>
          <div class="lp-hero__stat">
            <span class="lp-hero__stat-num">AI</span>
            <span class="lp-hero__stat-label">자동 보고서</span>
          </div>
          <div class="lp-hero__stat-divider"></div>
          <div class="lp-hero__stat">
            <span class="lp-hero__stat-num">7개</span>
            <span class="lp-hero__stat-label">분석 영역</span>
          </div>
          <div class="lp-hero__stat-divider"></div>
          <div class="lp-hero__stat">
            <span class="lp-hero__stat-num">70%</span>
            <span class="lp-hero__stat-label">업무 절감</span>
          </div>
        </div>
      </div>
      <div class="lp-hero__visual">
        <div class="lp-hero__screen lp-hero__screen--main">
          <%= image_tag 'promotional/screenshot_report.png', alt: '종합 보고서 화면', loading: 'eager' %>
        </div>
        <div class="lp-hero__screen lp-hero__screen--float">
          <%= image_tag 'promotional/screenshot_student_dashboard.png', alt: '학생 대시보드', loading: 'eager' %>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════ PROBLEM / SOLUTION ════════ -->
  <section class="lp-section lp-section--gray" id="why">
    <div class="lp-container">
      <div class="lp-section__header">
        <span class="lp-section__eyebrow">왜 ReadingPRO인가?</span>
        <h2 class="lp-section__title">읽기 교육, 이렇게 바뀝니다</h2>
      </div>
      <div class="lp-compare">
        <div class="lp-compare__col lp-compare__col--before">
          <h3>현재의 문제</h3>
          <ul>
            <li>학생 30명의 읽기 수준을 교사 1명이 파악하기 어려움</li>
            <li>진단 결과가 점수로만 제공되어 활용도 낮음</li>
            <li>읽기 교육이 일회성으로 끝남</li>
            <li>학부모가 자녀 읽기 수준을 모름</li>
            <li>학교 전체 읽기 현황 파악 불가</li>
          </ul>
        </div>
        <div class="lp-compare__arrow">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </div>
        <div class="lp-compare__col lp-compare__col--after">
          <h3>ReadingPRO의 해결</h3>
          <ul>
            <li>AI가 학생별 읽기 역량을 <strong>자동 진단</strong></li>
            <li><strong>7개 영역 종합 분석 보고서</strong> 자동 생성</li>
            <li><strong>3단계 발문 학습</strong>으로 지속적 사고력 신장</li>
            <li><strong>학부모 대시보드</strong>에서 실시간 성장 추적</li>
            <li><strong>학교 관리자 대시보드</strong>로 전체 현황 한눈에</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════ FEATURES SHOWCASE ════════ -->
  <section class="lp-section" id="features">
    <div class="lp-container">
      <div class="lp-section__header">
        <span class="lp-section__eyebrow">핵심 기능</span>
        <h2 class="lp-section__title">진단부터 성장까지, 하나의 플랫폼에서</h2>
        <p class="lp-section__desc">전문 연구진이 개발한 평가 문항 + AI 자동 채점 + 맞춤 피드백</p>
      </div>

      <!-- Feature 1 -->
      <div class="lp-feature">
        <div class="lp-feature__text">
          <div class="lp-feature__icon" style="background: #ede9fe; color: #7c3aed;">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/></svg>
          </div>
          <h3>AI 읽기 진단 평가</h3>
          <p>좌우 분할 화면으로 지문을 보면서 동시에 답변. 객관식+서술형 혼합 출제, 자동 저장, 타이머, 깃발 표시 등 학생 친화적 인터페이스를 제공합니다.</p>
          <ul class="lp-feature__list">
            <li>지문+문항 좌우 분할 화면</li>
            <li>자동 저장 (인터넷 끊김 대비)</li>
            <li>타이머 색상 코딩 (녹→노→빨)</li>
          </ul>
        </div>
        <div class="lp-feature__img">
          <%= image_tag 'promotional/screenshot_diagnosis.png', alt: '학생 진단 평가 화면' %>
        </div>
      </div>

      <!-- Feature 2 -->
      <div class="lp-feature lp-feature--reverse">
        <div class="lp-feature__text">
          <div class="lp-feature__icon" style="background: #dbeafe; color: #2563eb;">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
          </div>
          <h3>AI 종합 분석 보고서</h3>
          <p>GPT-4 기반 AI가 학생 응답 데이터를 분석하여 7개 섹션 보고서를 자동 작성합니다. 레이더 차트로 역량을 한눈에, PDF 다운로드로 상담에 즉시 활용합니다.</p>
          <ul class="lp-feature__list">
            <li>7개 섹션 종합 보고서</li>
            <li>레이더 차트 시각화</li>
            <li>교사 편집 + PDF 다운로드</li>
          </ul>
        </div>
        <div class="lp-feature__img">
          <%= image_tag 'promotional/screenshot_report.png', alt: '종합 보고서 화면' %>
        </div>
      </div>

      <!-- Feature 3 -->
      <div class="lp-feature">
        <div class="lp-feature__text">
          <div class="lp-feature__icon" style="background: #dcfce7; color: #16a34a;">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
          </div>
          <h3>발문을 통한 사고력 신장</h3>
          <p>3단계 발문 학습(책문열기→이야기나누기→삶적용)으로 텍스트 이해를 넘어 비판적 사고와 창의적 적용까지. AI가 즉각 피드백하고 수준별로 자동 조절합니다.</p>
          <ul class="lp-feature__list">
            <li>3단계: 책문열기 → 이야기나누기 → 삶적용</li>
            <li>AI 즉각 피드백 (강점/발전점)</li>
            <li>4수준 자동 스캐폴딩</li>
          </ul>
        </div>
        <div class="lp-feature__img">
          <%= image_tag 'promotional/screenshot_questioning2.png', alt: '발문 학습 모듈' %>
        </div>
      </div>

      <!-- Feature 4 -->
      <div class="lp-feature lp-feature--reverse">
        <div class="lp-feature__text">
          <div class="lp-feature__icon" style="background: #fef3c7; color: #d97706;">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M9 21V9"/></svg>
          </div>
          <h3>교사 피드백 대시보드</h3>
          <p>AI가 문항별 피드백 초안을 자동 생성하고 교사가 검토/수정합니다. 서술형 자동 채점, 일괄 생성, 일괄 배포로 교사 업무를 획기적으로 줄여줍니다.</p>
          <ul class="lp-feature__list">
            <li>AI 피드백 초안 → 교사 검토</li>
            <li>서술형 루브릭 기반 자동 채점</li>
            <li>일괄 생성 + 일괄 배포</li>
          </ul>
        </div>
        <div class="lp-feature__img">
          <%= image_tag 'promotional/screenshot_teacher_dashboard.png', alt: '교사 피드백 대시보드' %>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════ AUDIENCE TABS ════════ -->
  <section class="lp-section lp-section--dark" id="audience">
    <div class="lp-container">
      <div class="lp-section__header lp-section__header--light">
        <span class="lp-section__eyebrow" style="color: #c4b5fd;">대상별 안내</span>
        <h2 class="lp-section__title">누구를 위한 플랫폼인가요?</h2>
        <p class="lp-section__desc">역할에 맞는 기능과 가치를 확인하세요</p>
      </div>

      <div class="lp-tabs">
        <div class="lp-tabs__bar">
          <button class="lp-tabs__btn is-active" data-tab="admin">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M9 21V9"/></svg>
            학교 관리자
          </button>
          <button class="lp-tabs__btn" data-tab="teacher">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/></svg>
            교사 / 담당자
          </button>
          <button class="lp-tabs__btn" data-tab="parent">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
            학부모 / 학생
          </button>
        </div>

        <!-- TAB: 학교 관리자 -->
        <div class="lp-tabs__panel is-active" data-panel="admin">
          <div class="lp-tab-content">
            <div class="lp-tab-content__text">
              <h3>학교 전체 읽기 교육 현황을<br>데이터로 한눈에</h3>
              <p>교육청 보고, 학년별 비교 분석, 참여율 모니터링까지 — 학교 관리자가 필요한 모든 데이터를 실시간 대시보드로 제공합니다.</p>
              <div class="lp-tab-metrics">
                <div class="lp-tab-metric">
                  <span class="lp-tab-metric__num">무제한</span>
                  <span class="lp-tab-metric__label">학생 수</span>
                </div>
                <div class="lp-tab-metric">
                  <span class="lp-tab-metric__num">자동</span>
                  <span class="lp-tab-metric__label">계정 일괄 생성</span>
                </div>
                <div class="lp-tab-metric">
                  <span class="lp-tab-metric__num">실시간</span>
                  <span class="lp-tab-metric__label">참여율 모니터링</span>
                </div>
              </div>
              <ul class="lp-tab-checks">
                <li>학년별/학급별 평균 점수 비교 분석</li>
                <li>학생·학부모 계정 일괄 자동 생성</li>
                <li>개인정보 보호 설계 (학교 고유 ID)</li>
                <li>진단 참여율 및 완료 현황 모니터링</li>
                <li>피드백 배포 상태 추적</li>
              </ul>
            </div>
            <div class="lp-tab-content__img">
              <%= image_tag 'promotional/screenshot_teacher_dashboard.png', alt: '학교 관리 대시보드' %>
            </div>
          </div>
        </div>

        <!-- TAB: 교사 / 담당자 -->
        <div class="lp-tabs__panel" data-panel="teacher">
          <div class="lp-tab-content">
            <div class="lp-tab-content__text">
              <h3>AI가 초안을 작성하고,<br>교사가 검토합니다</h3>
              <p>300개 피드백 수작업, 서술형 채점 5시간, 보고서 15시간 — ReadingPRO가 이 모든 작업을 AI로 자동화하여 교사의 본질적 업무에 집중할 수 있게 합니다.</p>
              <div class="lp-tab-metrics">
                <div class="lp-tab-metric lp-tab-metric--accent">
                  <span class="lp-tab-metric__num">300개</span>
                  <span class="lp-tab-metric__label">피드백 자동 생성</span>
                </div>
                <div class="lp-tab-metric lp-tab-metric--accent">
                  <span class="lp-tab-metric__num">5시간→</span>
                  <span class="lp-tab-metric__label">AI 자동 채점</span>
                </div>
                <div class="lp-tab-metric lp-tab-metric--accent">
                  <span class="lp-tab-metric__num">15시간→</span>
                  <span class="lp-tab-metric__label">보고서 자동 생성</span>
                </div>
              </div>
              <ul class="lp-tab-checks">
                <li>AI 피드백 초안 생성 → 교사 검토/수정</li>
                <li>루브릭 기반 서술형 자동 채점</li>
                <li>7개 섹션 종합 보고서 원클릭 생성</li>
                <li>학부모 상담 자료 PDF 즉시 활용</li>
                <li>발문 학습 지도 및 이중 검토 시스템</li>
              </ul>
            </div>
            <div class="lp-tab-content__img">
              <%= image_tag 'promotional/screenshot_report.png', alt: '교사 보고서 관리' %>
            </div>
          </div>
        </div>

        <!-- TAB: 학부모 / 학생 -->
        <div class="lp-tabs__panel" data-panel="parent">
          <div class="lp-tab-content">
            <div class="lp-tab-content__text">
              <h3>우리 아이 읽기 성장을<br>눈으로 확인하세요</h3>
              <p>자녀의 읽기 역량 프로필, 성장 추이 그래프, 종합 보고서를 학부모 대시보드에서 실시간으로 확인할 수 있습니다. AI 즉각 피드백으로 자기주도 학습도 가능합니다.</p>
              <div class="lp-tab-metrics">
                <div class="lp-tab-metric">
                  <span class="lp-tab-metric__num">실시간</span>
                  <span class="lp-tab-metric__label">성장 추이 그래프</span>
                </div>
                <div class="lp-tab-metric">
                  <span class="lp-tab-metric__num">즉각</span>
                  <span class="lp-tab-metric__label">AI 피드백</span>
                </div>
                <div class="lp-tab-metric">
                  <span class="lp-tab-metric__num">맞춤</span>
                  <span class="lp-tab-metric__label">수준별 발문</span>
                </div>
              </div>
              <ul class="lp-tab-checks">
                <li>자녀 읽기 역량 프로필 시각적 확인</li>
                <li>진단별 성장 추이 그래프</li>
                <li>종합 보고서 열람 (7개 섹션)</li>
                <li>AI 즉각 피드백 자기주도 학습</li>
                <li>수준별 맞춤 발문으로 사고력 신장</li>
              </ul>
            </div>
            <div class="lp-tab-content__img">
              <%= image_tag 'promotional/screenshot_student_dashboard.png', alt: '학생/학부모 대시보드' %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════ ADOPTION PROCESS ════════ -->
  <section class="lp-section" id="process">
    <div class="lp-container">
      <div class="lp-section__header">
        <span class="lp-section__eyebrow">도입 절차</span>
        <h2 class="lp-section__title">4단계로 간편하게 도입하세요</h2>
        <p class="lp-section__desc">설치 불필요, 웹 기반 — URL 접속만으로 바로 사용</p>
      </div>
      <div class="lp-process">
        <div class="lp-process__step">
          <div class="lp-process__num">1</div>
          <h4>학교 등록</h4>
          <p>학교 정보 입력<br>담당 교사 지정</p>
          <span class="lp-process__time">1일</span>
        </div>
        <div class="lp-process__connector">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#cbd5e1" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </div>
        <div class="lp-process__step">
          <div class="lp-process__num">2</div>
          <h4>계정 생성</h4>
          <p>학생/학부모 계정<br>일괄 자동 생성</p>
          <span class="lp-process__time">즉시</span>
        </div>
        <div class="lp-process__connector">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#cbd5e1" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </div>
        <div class="lp-process__step">
          <div class="lp-process__num">3</div>
          <h4>교사 연수</h4>
          <p>플랫폼 사용법<br>피드백 작성법</p>
          <span class="lp-process__time">2시간</span>
        </div>
        <div class="lp-process__connector">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#cbd5e1" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </div>
        <div class="lp-process__step">
          <div class="lp-process__num">4</div>
          <h4>진단 실시</h4>
          <p>진단 평가 배정<br>결과 분석/활용</p>
          <span class="lp-process__time">자율</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════ TRUST / SECURITY ════════ -->
  <section class="lp-section lp-section--gray">
    <div class="lp-container">
      <div class="lp-section__header">
        <span class="lp-section__eyebrow">안심 설계</span>
        <h2 class="lp-section__title">개인정보 보호 & 기술 요구사항</h2>
      </div>
      <div class="lp-trust-grid">
        <div class="lp-trust-card">
          <div class="lp-trust-card__icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#7c3aed" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
          </div>
          <h4>실명 미사용</h4>
          <p>학교 고유 ID로 대체<br>(예: shinmyung_S-0001)</p>
        </div>
        <div class="lp-trust-card">
          <div class="lp-trust-card__icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#2563eb" stroke-width="2"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
          </div>
          <h4>암호화 저장</h4>
          <p>모든 데이터 암호화<br>최초 로그인 시 비밀번호 변경 필수</p>
        </div>
        <div class="lp-trust-card">
          <div class="lp-trust-card__icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#16a34a" stroke-width="2"><rect x="2" y="3" width="20" height="14" rx="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
          </div>
          <h4>설치 불필요</h4>
          <p>PC, 태블릿, 스마트폰<br>Chrome, Edge, Safari</p>
        </div>
        <div class="lp-trust-card">
          <div class="lp-trust-card__icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#d97706" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M2 12h20M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>
          </div>
          <h4>웹 기반</h4>
          <p>일반 학교 Wi-Fi 환경<br>URL 접속만으로 사용</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════ FINAL CTA ════════ -->
  <section class="lp-cta">
    <div class="lp-container lp-cta__inner">
      <h2>지금, 데이터 기반 읽기 교육을 시작하세요</h2>
      <p>교육청·학교·도서관 정기 진단 프로그램부터 학부모 상담 자료까지,<br>읽기 역량을 근거 기반으로 관리할 수 있는 표준 리포트를 제공합니다.</p>
      <div class="lp-cta__actions">
        <a href="<%= login_path %>" class="lp-btn lp-btn--white lp-btn--lg">플랫폼 시작하기</a>
      </div>
    </div>
  </section>

  <!-- ════════ FOOTER ════════ -->
  <footer class="lp-footer">
    <div class="lp-container lp-footer__inner">
      <div class="lp-footer__brand">
        <%= image_tag 'readingpro_logo.png', alt: 'ReadingPRO', style: 'height: 36px; width: auto; filter: brightness(0) invert(1); opacity: 0.7;' %>
        <p>AI와 함께하는 읽기 교육의 새로운 기준</p>
      </div>
      <div class="lp-footer__links">
        <div>
          <h5>제품</h5>
          <a href="#features">주요 기능</a>
          <a href="#audience">대상별 안내</a>
          <a href="#process">도입 절차</a>
        </div>
        <div>
          <h5>지원</h5>
          <a href="<%= login_path %>">로그인</a>
          <a href="#">시연 요청</a>
          <a href="#">문의하기</a>
        </div>
      </div>
    </div>
    <div class="lp-footer__bottom">
      <div class="lp-container">&copy; 2026 ReadingPRO &mdash; (사)전국독서새물결모임. All rights reserved.</div>
    </div>
  </footer>
</div>

<style>
/* ═══════════════════════════════════════════
   LANDING PAGE — ReadingPRO
   Modern, persuasive, audience-tabbed design
   ═══════════════════════════════════════════ */

/* ── Reset & Base ── */
.lp { --lp-primary: #667eea; --lp-secondary: #764ba2; --lp-grad: linear-gradient(135deg, #667eea 0%, #764ba2 100%); --lp-dark: #0f172a; --lp-text: #334155; --lp-muted: #64748b; --lp-light: #f8fafc; --lp-border: #e2e8f0; --lp-radius: 12px; --lp-transition: 0.3s cubic-bezier(0.4, 0, 0.2, 1); font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; color: var(--lp-text); line-height: 1.6; -webkit-font-smoothing: antialiased; }
.lp *, .lp *::before, .lp *::after { box-sizing: border-box; margin: 0; padding: 0; }
.lp-container { max-width: 1200px; margin: 0 auto; padding: 0 24px; }

/* ── Header ── */
.lp-header { position: sticky; top: 0; z-index: 100; background: rgba(255,255,255,0.92); backdrop-filter: blur(12px); border-bottom: 1px solid var(--lp-border); }
.lp-header__inner { display: flex; align-items: center; justify-content: space-between; height: 72px; }
.lp-header__logo { display: flex; align-items: center; text-decoration: none; }
.lp-header__nav { display: flex; gap: 32px; }
.lp-header__link { color: var(--lp-muted); text-decoration: none; font-size: 15px; font-weight: 500; transition: color var(--lp-transition); }
.lp-header__link:hover { color: var(--lp-primary); }
@media (max-width: 768px) { .lp-header__nav { display: none; } }

/* ── Buttons ── */
.lp-btn { display: inline-flex; align-items: center; justify-content: center; gap: 8px; padding: 10px 24px; border-radius: 8px; font-size: 15px; font-weight: 600; text-decoration: none; border: none; cursor: pointer; transition: all var(--lp-transition); white-space: nowrap; }
.lp-btn--primary { background: var(--lp-grad); color: white; box-shadow: 0 4px 14px rgba(102,126,234,0.35); }
.lp-btn--primary:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(102,126,234,0.45); }
.lp-btn--white { background: white; color: var(--lp-primary); box-shadow: 0 4px 14px rgba(0,0,0,0.15); }
.lp-btn--white:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(0,0,0,0.2); }
.lp-btn--ghost-white { background: transparent; color: white; border: 2px solid rgba(255,255,255,0.5); }
.lp-btn--ghost-white:hover { background: rgba(255,255,255,0.1); border-color: white; }
.lp-btn--sm { padding: 8px 18px; font-size: 14px; }
.lp-btn--lg { padding: 14px 32px; font-size: 16px; border-radius: 10px; }

/* ── Announcements ── */
.lp-announce { background: var(--lp-grad); padding: 10px 0; }
.lp-announce__item { display: flex; align-items: center; gap: 10px; color: white; font-size: 14px; font-weight: 500; padding: 6px 14px; background: rgba(255,255,255,0.12); border-radius: 8px; margin-bottom: 6px; }
.lp-announce__item:last-child { margin-bottom: 0; }

/* ── Hero ── */
.lp-hero { position: relative; overflow: hidden; background: var(--lp-grad); padding: 100px 0 80px; color: white; }
.lp-hero__bg { position: absolute; inset: 0; pointer-events: none; }
.lp-hero__orb { position: absolute; border-radius: 50%; filter: blur(80px); opacity: 0.35; }
.lp-hero__orb--1 { width: 600px; height: 600px; background: #a78bfa; top: -200px; right: -100px; }
.lp-hero__orb--2 { width: 400px; height: 400px; background: #6366f1; bottom: -150px; left: -100px; }
.lp-hero__orb--3 { width: 300px; height: 300px; background: #ec4899; top: 50%; left: 50%; transform: translate(-50%, -50%); }
.lp-hero__inner { position: relative; display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: center; }
.lp-hero__badge { display: inline-block; padding: 6px 16px; background: rgba(255,255,255,0.15); border: 1px solid rgba(255,255,255,0.25); border-radius: 100px; font-size: 13px; font-weight: 600; letter-spacing: 0.5px; margin-bottom: 20px; backdrop-filter: blur(4px); }
.lp-hero__title { font-size: 48px; font-weight: 800; line-height: 1.2; margin-bottom: 20px; letter-spacing: -0.5px; }
.lp-hero__title em { font-style: normal; background: linear-gradient(to right, #e0e7ff, #c4b5fd); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
.lp-hero__subtitle { font-size: 17px; line-height: 1.7; opacity: 0.9; margin-bottom: 32px; max-width: 480px; }
.lp-hero__cta { display: flex; gap: 14px; margin-bottom: 48px; flex-wrap: wrap; }
.lp-hero__stats { display: flex; gap: 0; }
.lp-hero__stat { text-align: center; padding: 0 20px; }
.lp-hero__stat:first-child { padding-left: 0; }
.lp-hero__stat-num { display: block; font-size: 24px; font-weight: 800; }
.lp-hero__stat-label { display: block; font-size: 12px; opacity: 0.75; margin-top: 2px; }
.lp-hero__stat-divider { width: 1px; background: rgba(255,255,255,0.25); }

.lp-hero__visual { position: relative; }
.lp-hero__screen { border-radius: 12px; overflow: hidden; box-shadow: 0 20px 60px rgba(0,0,0,0.3); }
.lp-hero__screen img { width: 100%; height: auto; display: block; }
.lp-hero__screen--main { transform: perspective(1000px) rotateY(-5deg) rotateX(2deg); }
.lp-hero__screen--float { position: absolute; width: 55%; bottom: -30px; left: -40px; border: 3px solid rgba(255,255,255,0.2); transform: perspective(1000px) rotateY(5deg); }

@media (max-width: 1024px) {
  .lp-hero__inner { grid-template-columns: 1fr; text-align: center; }
  .lp-hero__subtitle { margin-left: auto; margin-right: auto; }
  .lp-hero__cta { justify-content: center; }
  .lp-hero__stats { justify-content: center; }
  .lp-hero__visual { display: none; }
  .lp-hero__title { font-size: 36px; }
}

/* ── Sections ── */
.lp-section { padding: 100px 0; }
.lp-section--gray { background: var(--lp-light); }
.lp-section--dark { background: var(--lp-dark); color: white; }
.lp-section__header { text-align: center; margin-bottom: 60px; }
.lp-section__header--light .lp-section__title { color: white; }
.lp-section__header--light .lp-section__desc { color: rgba(255,255,255,0.7); }
.lp-section__eyebrow { display: inline-block; font-size: 13px; font-weight: 700; text-transform: uppercase; letter-spacing: 1.5px; color: var(--lp-primary); margin-bottom: 12px; }
.lp-section__title { font-size: 36px; font-weight: 800; color: var(--lp-dark); line-height: 1.3; margin-bottom: 12px; letter-spacing: -0.3px; }
.lp-section__desc { font-size: 17px; color: var(--lp-muted); max-width: 600px; margin: 0 auto; }

/* ── Compare (Before/After) ── */
.lp-compare { display: grid; grid-template-columns: 1fr auto 1fr; gap: 32px; align-items: start; }
.lp-compare__col { padding: 32px; border-radius: var(--lp-radius); }
.lp-compare__col h3 { font-size: 18px; font-weight: 700; margin-bottom: 20px; }
.lp-compare__col ul { list-style: none; display: flex; flex-direction: column; gap: 14px; }
.lp-compare__col li { font-size: 15px; line-height: 1.6; padding-left: 28px; position: relative; }
.lp-compare__col--before { background: #fef2f2; border: 1px solid #fecaca; }
.lp-compare__col--before h3 { color: #dc2626; }
.lp-compare__col--before li::before { content: "✕"; position: absolute; left: 0; color: #ef4444; font-weight: 700; }
.lp-compare__col--after { background: #f0fdf4; border: 1px solid #bbf7d0; }
.lp-compare__col--after h3 { color: #16a34a; }
.lp-compare__col--after li::before { content: "✓"; position: absolute; left: 0; color: #16a34a; font-weight: 700; }
.lp-compare__arrow { display: flex; align-items: center; color: var(--lp-muted); padding-top: 60px; }
@media (max-width: 768px) {
  .lp-compare { grid-template-columns: 1fr; }
  .lp-compare__arrow { justify-content: center; padding: 0; transform: rotate(90deg); }
}

/* ── Features Showcase ── */
.lp-feature { display: grid; grid-template-columns: 1fr 1.2fr; gap: 60px; align-items: center; margin-bottom: 80px; }
.lp-feature:last-child { margin-bottom: 0; }
.lp-feature--reverse { direction: rtl; }
.lp-feature--reverse > * { direction: ltr; }
.lp-feature__icon { width: 48px; height: 48px; border-radius: 12px; display: flex; align-items: center; justify-content: center; margin-bottom: 20px; }
.lp-feature__text h3 { font-size: 26px; font-weight: 800; color: var(--lp-dark); margin-bottom: 14px; line-height: 1.3; }
.lp-feature__text p { font-size: 16px; color: var(--lp-muted); line-height: 1.7; margin-bottom: 20px; }
.lp-feature__list { list-style: none; display: flex; flex-direction: column; gap: 10px; }
.lp-feature__list li { font-size: 14px; font-weight: 500; color: var(--lp-text); padding-left: 24px; position: relative; }
.lp-feature__list li::before { content: ""; position: absolute; left: 0; top: 6px; width: 8px; height: 8px; border-radius: 50%; background: var(--lp-primary); }
.lp-feature__img { border-radius: var(--lp-radius); overflow: hidden; box-shadow: 0 8px 40px rgba(0,0,0,0.12); border: 1px solid var(--lp-border); }
.lp-feature__img img { width: 100%; height: auto; display: block; }
@media (max-width: 768px) {
  .lp-feature, .lp-feature--reverse { grid-template-columns: 1fr; direction: ltr; gap: 32px; }
}

/* ── Tabs ── */
.lp-tabs__bar { display: flex; justify-content: center; gap: 8px; margin-bottom: 48px; background: rgba(255,255,255,0.08); padding: 6px; border-radius: 14px; max-width: 600px; margin-left: auto; margin-right: auto; }
.lp-tabs__btn { display: flex; align-items: center; gap: 8px; padding: 12px 24px; border: none; border-radius: 10px; background: transparent; color: rgba(255,255,255,0.6); font-size: 15px; font-weight: 600; cursor: pointer; transition: all var(--lp-transition); white-space: nowrap; }
.lp-tabs__btn:hover { color: white; background: rgba(255,255,255,0.08); }
.lp-tabs__btn.is-active { background: white; color: var(--lp-primary); box-shadow: 0 4px 14px rgba(0,0,0,0.15); }
.lp-tabs__panel { display: none; animation: lpFadeUp 0.4s ease; }
.lp-tabs__panel.is-active { display: block; }
@keyframes lpFadeUp { from { opacity: 0; transform: translateY(12px); } to { opacity: 1; transform: translateY(0); } }

.lp-tab-content { display: grid; grid-template-columns: 1fr 1.1fr; gap: 48px; align-items: center; }
.lp-tab-content__text h3 { font-size: 28px; font-weight: 800; color: white; line-height: 1.35; margin-bottom: 16px; }
.lp-tab-content__text > p { font-size: 16px; color: rgba(255,255,255,0.75); line-height: 1.7; margin-bottom: 28px; }
.lp-tab-content__img { border-radius: var(--lp-radius); overflow: hidden; box-shadow: 0 12px 48px rgba(0,0,0,0.35); border: 1px solid rgba(255,255,255,0.1); }
.lp-tab-content__img img { width: 100%; height: auto; display: block; }

.lp-tab-metrics { display: flex; gap: 20px; margin-bottom: 28px; flex-wrap: wrap; }
.lp-tab-metric { background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.12); border-radius: 10px; padding: 14px 18px; text-align: center; flex: 1; min-width: 110px; }
.lp-tab-metric--accent { background: rgba(102,126,234,0.15); border-color: rgba(102,126,234,0.3); }
.lp-tab-metric__num { display: block; font-size: 20px; font-weight: 800; color: white; }
.lp-tab-metric__label { display: block; font-size: 12px; color: rgba(255,255,255,0.6); margin-top: 4px; }

.lp-tab-checks { list-style: none; display: flex; flex-direction: column; gap: 10px; }
.lp-tab-checks li { font-size: 14px; color: rgba(255,255,255,0.85); padding-left: 28px; position: relative; }
.lp-tab-checks li::before { content: "✓"; position: absolute; left: 0; color: #a78bfa; font-weight: 700; font-size: 15px; }

@media (max-width: 768px) {
  .lp-tabs__bar { flex-direction: column; max-width: 300px; }
  .lp-tab-content { grid-template-columns: 1fr; }
  .lp-tab-content__text h3 { font-size: 22px; }
}

/* ── Process Steps ── */
.lp-process { display: flex; align-items: flex-start; justify-content: center; gap: 0; flex-wrap: wrap; }
.lp-process__step { text-align: center; flex: 0 1 200px; }
.lp-process__num { width: 52px; height: 52px; border-radius: 50%; background: var(--lp-grad); color: white; font-size: 20px; font-weight: 800; display: flex; align-items: center; justify-content: center; margin: 0 auto 16px; box-shadow: 0 4px 14px rgba(102,126,234,0.35); }
.lp-process__step h4 { font-size: 17px; font-weight: 700; color: var(--lp-dark); margin-bottom: 8px; }
.lp-process__step p { font-size: 14px; color: var(--lp-muted); line-height: 1.5; margin-bottom: 12px; }
.lp-process__time { display: inline-block; padding: 4px 12px; border-radius: 100px; background: #ede9fe; color: #7c3aed; font-size: 12px; font-weight: 700; }
.lp-process__connector { display: flex; align-items: center; padding-top: 14px; }
@media (max-width: 768px) {
  .lp-process { flex-direction: column; align-items: center; gap: 16px; }
  .lp-process__connector { transform: rotate(90deg); padding: 0; }
}

/* ── Trust Grid ── */
.lp-trust-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 24px; }
.lp-trust-card { background: white; border: 1px solid var(--lp-border); border-radius: var(--lp-radius); padding: 28px; text-align: center; transition: all var(--lp-transition); }
.lp-trust-card:hover { transform: translateY(-4px); box-shadow: 0 8px 28px rgba(0,0,0,0.08); }
.lp-trust-card__icon { width: 52px; height: 52px; border-radius: 14px; background: var(--lp-light); display: flex; align-items: center; justify-content: center; margin: 0 auto 16px; }
.lp-trust-card h4 { font-size: 16px; font-weight: 700; color: var(--lp-dark); margin-bottom: 8px; }
.lp-trust-card p { font-size: 14px; color: var(--lp-muted); line-height: 1.5; }
@media (max-width: 768px) { .lp-trust-grid { grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 480px) { .lp-trust-grid { grid-template-columns: 1fr; } }

/* ── Final CTA ── */
.lp-cta { background: var(--lp-grad); padding: 80px 0; text-align: center; color: white; }
.lp-cta h2 { font-size: 32px; font-weight: 800; margin-bottom: 16px; }
.lp-cta p { font-size: 16px; opacity: 0.85; line-height: 1.7; margin-bottom: 32px; }
.lp-cta__actions { display: flex; justify-content: center; gap: 14px; flex-wrap: wrap; }

/* ── Footer ── */
.lp-footer { background: var(--lp-dark); color: rgba(255,255,255,0.6); padding-top: 60px; }
.lp-footer__inner { display: flex; justify-content: space-between; gap: 40px; padding-bottom: 40px; border-bottom: 1px solid rgba(255,255,255,0.1); }
.lp-footer__brand p { font-size: 14px; margin-top: 12px; max-width: 280px; }
.lp-footer__links { display: flex; gap: 60px; }
.lp-footer__links h5 { color: white; font-size: 14px; font-weight: 700; margin-bottom: 16px; }
.lp-footer__links a { display: block; font-size: 14px; color: rgba(255,255,255,0.5); text-decoration: none; margin-bottom: 10px; transition: color var(--lp-transition); }
.lp-footer__links a:hover { color: white; }
.lp-footer__bottom { padding: 20px 0; font-size: 13px; text-align: center; }
@media (max-width: 768px) {
  .lp-footer__inner { flex-direction: column; }
  .lp-footer__links { gap: 40px; }
}
</style>

<script>
(function() {
  // Tab switching
  var tabBtns = document.querySelectorAll('.lp-tabs__btn');
  var tabPanels = document.querySelectorAll('.lp-tabs__panel');

  tabBtns.forEach(function(btn) {
    btn.addEventListener('click', function() {
      var target = btn.dataset.tab;
      tabBtns.forEach(function(b) { b.classList.remove('is-active'); });
      tabPanels.forEach(function(p) { p.classList.remove('is-active'); });
      btn.classList.add('is-active');
      var panel = document.querySelector('[data-panel="' + target + '"]');
      if (panel) panel.classList.add('is-active');
    });
  });

  // Smooth scroll for anchor links
  document.querySelectorAll('.lp a[href^="#"]').forEach(function(a) {
    a.addEventListener('click', function(e) {
      var target = document.querySelector(a.getAttribute('href'));
      if (target) {
        e.preventDefault();
        target.scrollIntoView({ behavior: 'smooth', block: 'start' });
      }
    });
  });

  // Header shadow on scroll
  var header = document.querySelector('.lp-header');
  if (header) {
    window.addEventListener('scroll', function() {
      header.style.boxShadow = window.scrollY > 10 ? '0 2px 20px rgba(0,0,0,0.08)' : 'none';
    }, { passive: true });
  }
})();
</script>
