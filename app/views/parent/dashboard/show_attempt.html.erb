<% content_for :portal_title do %>
  시험 결과 상세
<% end %>

<% content_for :portal_subtitle do %>
  객관식 및 서술형 문항별 답변 및 평가 피드백
<% end %>

<% content_for :portal_nav do %>
  <%= render "parent/shared/nav", current: :reports %>
<% end %>

<style>
  .attempt-header {
    padding: 24px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border-radius: 8px;
    margin-bottom: 24px;
  }

  .attempt-header h1 {
    margin: 0 0 12px 0;
    font-size: var(--rp-font-size-2xl);
  }

  .attempt-info {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 16px;
    margin-top: 16px;
  }

  .info-item {
    background: rgba(255, 255, 255, 0.15);
    padding: 12px;
    border-radius: 4px;
  }

  .info-label {
    font-size: var(--rp-font-size-xs);
    opacity: 0.9;
    margin-bottom: 4px;
  }

  .info-value {
    font-size: var(--rp-font-size-md);
    font-weight: 600;
  }

  .section-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin: 32px 0 16px 0;
  }

  .section-title {
    font-size: var(--rp-font-size-xl);
    font-weight: 600;
    color: #333;
  }

  .section-count {
    background: #667eea;
    color: white;
    padding: 4px 12px;
    border-radius: 20px;
    font-size: var(--rp-font-size-xs);
    font-weight: 600;
  }

  /* 테이블 스타일 */
  .result-table {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 24px;
    background: white;
    border: 1px solid #e0e0e0;
    border-radius: 8px;
    overflow: hidden;
  }

  .result-table thead {
    background: #f8f9fa;
    border-bottom: 2px solid #dee2e6;
  }

  .result-table th {
    padding: 16px 12px;
    text-align: left;
    font-weight: 600;
    font-size: var(--rp-font-size-xs);
    color: #333;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .result-table td {
    padding: 16px 12px;
    border-bottom: 1px solid #dee2e6;
    font-size: var(--rp-font-size-sm);
  }

  .result-table tbody tr:last-child td {
    border-bottom: none;
  }

  .result-table tbody tr:hover {
    background: #f8f9fa;
  }

  /* 테이블 셀 스타일 */
  .cell-number {
    font-weight: 600;
    color: #667eea;
    width: 60px;
  }

  .cell-indicator {
    font-weight: 500;
    color: #333;
    min-width: 100px;
  }

  .cell-sub-indicator {
    color: #666;
    font-size: var(--rp-font-size-xs);
  }

  .cell-answer {
    max-width: 300px;
    line-height: 1.5;
  }

  .cell-question {
    max-width: 400px;
    line-height: 1.5;
    color: #333;
  }

  .cell-feedback {
    max-width: 350px;
    line-height: 1.5;
    color: #333;
  }

  .correct-badge {
    display: inline-block;
    background: #d4edda;
    color: #155724;
    padding: 4px 8px;
    border-radius: 4px;
    font-size: var(--rp-font-size-2xs);
    font-weight: 600;
  }

  .incorrect-badge {
    display: inline-block;
    background: #f8d7da;
    color: #721c24;
    padding: 4px 8px;
    border-radius: 4px;
    font-size: var(--rp-font-size-2xs);
    font-weight: 600;
  }

  .score-badge {
    display: inline-block;
    background: #d1ecf1;
    color: #0c5460;
    padding: 4px 8px;
    border-radius: 4px;
    font-size: var(--rp-font-size-xs);
    font-weight: 600;
  }

  .feedback-expand {
    cursor: pointer;
    color: #667eea;
    text-decoration: underline;
    font-size: var(--rp-font-size-xs);
  }

  .feedback-modal {
    display: none;
    position: fixed;
    z-index: 1000;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5);
  }

  .feedback-modal-content {
    background-color: white;
    margin: 5% auto;
    padding: 24px;
    border-radius: 8px;
    width: 80%;
    max-width: 600px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }

  .feedback-modal-header {
    font-size: var(--rp-font-size-md);
    font-weight: 600;
    margin-bottom: 16px;
    color: #333;
  }

  .feedback-modal-body {
    font-size: var(--rp-font-size-sm);
    line-height: 1.6;
    color: #666;
    margin-bottom: 16px;
    max-height: 400px;
    overflow-y: auto;
  }

  .feedback-modal-footer {
    text-align: right;
  }

  .close-btn {
    background: #667eea;
    color: white;
    border: none;
    padding: 8px 16px;
    border-radius: 4px;
    cursor: pointer;
    font-size: var(--rp-font-size-xs);
  }

  .close-btn:hover {
    background: #764ba2;
  }

  .back-link {
    display: inline-block;
    margin-bottom: 16px;
    color: #667eea;
    text-decoration: none;
    font-size: var(--rp-font-size-base);
  }

  .back-link:hover {
    text-decoration: underline;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 16px;
    margin-bottom: 24px;
  }

  .stat-card {
    background: white;
    border: 1px solid #e0e0e0;
    border-radius: 8px;
    padding: 16px;
    text-align: center;
  }

  .stat-number {
    font-size: var(--rp-font-size-3xl);
    font-weight: 700;
    color: #667eea;
    margin-bottom: 4px;
  }

  .stat-label {
    font-size: var(--rp-font-size-xs);
    color: #999;
  }

  @media (max-width: 768px) {
    .result-table {
      font-size: var(--rp-font-size-2xs);
    }

    .result-table th,
    .result-table td {
      padding: 12px 8px;
    }

    .cell-question,
    .cell-answer,
    .cell-feedback {
      max-width: 200px;
    }
  }
</style>

<a href="<%= parent_show_report_path(@attempt.id, student_id: @student.id) %>" class="back-link">← 리포트로 돌아가기</a>

<div class="attempt-header">
  <h1>시험 결과 상세</h1>
  <div class="attempt-info">
    <div class="info-item">
      <div class="info-label">진단 번호</div>
      <div class="info-value">#<%= @attempt.id %></div>
    </div>
    <div class="info-item">
      <div class="info-label">시험 상태</div>
      <div class="info-value"><%= @attempt.status == 'completed' ? '완료' : '진행중' %></div>
    </div>
    <div class="info-item">
      <div class="info-label">시작일시</div>
      <div class="info-value"><%= @attempt.started_at&.strftime("%m월 %d일") %></div>
    </div>
    <div class="info-item">
      <div class="info-label">총 문항</div>
      <div class="info-value"><%= @responses.count %></div>
    </div>
  </div>
</div>

<!-- 통계 -->
<div class="stats-grid">
  <div class="stat-card">
    <div class="stat-number"><%= @mcq_responses.count %></div>
    <div class="stat-label">객관식 문항</div>
  </div>
  <div class="stat-card">
    <div class="stat-number"><%= @constructed_responses.count %></div>
    <div class="stat-label">서술형 문항</div>
  </div>
  <% correct_count = @mcq_responses.select { |r| r.selected_choice&.is_correct }.count %>
  <div class="stat-card">
    <div class="stat-number"><%= correct_count %></div>
    <div class="stat-label">객관식 정답</div>
  </div>
</div>

<!-- 객관식 섹션 -->
<% if @mcq_responses.any? %>
  <div class="section-header">
    <h2 class="section-title">📋 객관식 문항</h2>
    <span class="section-count"><%= @mcq_responses.count %>문항</span>
  </div>

  <table class="result-table">
    <thead>
      <tr>
        <th style="width: 60px;">문항번호</th>
        <th style="width: 120px;">평가지표</th>
        <th style="width: 120px;">하위지표</th>
        <th style="width: 100px;">정답</th>
        <th style="width: 100px;">학생정오</th>
        <th>해석 및 피드백</th>
      </tr>
    </thead>
    <tbody>
      <% @mcq_responses.each_with_index do |response, idx| %>
        <% is_correct = response.selected_choice&.is_correct %>
        <tr>
          <td class="cell-number"><%= idx + 1 %></td>
          <td class="cell-indicator"><%= response.item.evaluation_indicator&.name %></td>
          <td class="cell-sub-indicator"><%= response.item.sub_indicator&.name %></td>
          <td class="cell-answer">
            <%= response.item.item_choices.find(&:is_correct)&.choice_letter %>.
            <span style="color: #666; font-size: var(--rp-font-size-xs);">
              <%= response.item.item_choices.find(&:is_correct)&.choice_text&.slice(0, 30) %>...
            </span>
          </td>
          <td class="cell-answer">
            <% if is_correct %>
              <span class="correct-badge">✓ 정답</span>
            <% else %>
              <span class="incorrect-badge">✗ 오답</span>
              <br>
              <span style="color: #999; font-size: var(--rp-font-size-2xs);">
                선택: <%= response.selected_choice&.choice_letter %>
              </span>
            <% end %>
          </td>
          <td class="cell-feedback">
            <% if response.response_feedbacks.any? %>
              <span class="feedback-expand" onclick="showFeedback(<%= idx %>, 'mcq')">
                💬 피드백보기
              </span>
              <div id="feedback-modal-<%= idx %>-mcq" class="feedback-modal">
                <div class="feedback-modal-content">
                  <div class="feedback-modal-header">
                    문항 <%= idx + 1 %> - 피드백
                  </div>
                  <div class="feedback-modal-body">
                    <% response.response_feedbacks.each do |feedback| %>
                      <p><strong><%= feedback.source %></strong></p>
                      <%= simple_format(feedback.feedback_text) %>
                      <hr style="margin: 12px 0; border: none; border-top: 1px solid #ddd;">
                    <% end %>
                  </div>
                  <div class="feedback-modal-footer">
                    <button class="close-btn" onclick="closeFeedback(<%= idx %>, 'mcq')">닫기</button>
                  </div>
                </div>
              </div>
            <% else %>
              <span style="color: #999; font-size: var(--rp-font-size-xs);">피드백 없음</span>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>

<!-- 서술형 섹션 -->
<% if @constructed_responses.any? %>
  <div class="section-header">
    <h2 class="section-title">✍️ 서술형 문항</h2>
    <span class="section-count"><%= @constructed_responses.count %>문항</span>
  </div>

  <table class="result-table">
    <thead>
      <tr>
        <th style="width: 60px;">문항번호</th>
        <th style="width: 150px;">의사소통능력</th>
        <th style="width: 150px;">표현과 전달능력</th>
        <th style="width: 100px;">평가기준</th>
        <th style="width: 80px;">평가점</th>
        <th>종합 피드백</th>
      </tr>
    </thead>
    <tbody>
      <% @constructed_responses.each_with_index do |response, idx| %>
        <tr>
          <td class="cell-number"><%= @mcq_responses.count + idx + 1 %></td>
          <td class="cell-indicator"><%= response.item.evaluation_indicator&.name %></td>
          <td class="cell-sub-indicator"><%= response.item.sub_indicator&.name %></td>
          <td class="cell-question">
            <strong>Q:</strong>
            <br>
            <%= response.item.prompt&.slice(0, 100) %>...
          </td>
          <td class="cell-answer">
            <% if response.response_rubric_scores.any? %>
              <% response.response_rubric_scores.each do |score| %>
                <span class="score-badge">
                  <%= score.level_score %> / <%= score.rubric_criterion&.rubric&.max_score || 5 %>
                </span>
                <br>
              <% end %>
            <% else %>
              <span style="color: #999; font-size: var(--rp-font-size-xs);">평가 대기</span>
            <% end %>
          </td>
          <td class="cell-feedback">
            <% if response.response_feedbacks.any? %>
              <span class="feedback-expand" onclick="showFeedback(<%= idx %>, 'constructed')">
                💬 피드백보기
              </span>
              <div id="feedback-modal-<%= idx %>-constructed" class="feedback-modal">
                <div class="feedback-modal-content">
                  <div class="feedback-modal-header">
                    문항 <%= @mcq_responses.count + idx + 1 %> - 종합 피드백
                  </div>
                  <div class="feedback-modal-body">
                    <p><strong>학생 답변:</strong></p>
                    <div style="background: #f8f9fa; padding: 12px; border-radius: 4px; margin-bottom: 16px;">
                      <%= simple_format(response.answer_text || "(답변 없음)") %>
                    </div>

                    <% response.response_feedbacks.each do |feedback| %>
                      <p><strong><%= feedback.source %></strong></p>
                      <%= simple_format(feedback.feedback_text) %>
                      <hr style="margin: 12px 0; border: none; border-top: 1px solid #ddd;">
                    <% end %>
                  </div>
                  <div class="feedback-modal-footer">
                    <button class="close-btn" onclick="closeFeedback(<%= idx %>, 'constructed')">닫기</button>
                  </div>
                </div>
              </div>
            <% else %>
              <span style="color: #999; font-size: var(--rp-font-size-xs);">피드백 없음</span>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>

<% if @responses.empty? %>
  <div style="text-align: center; padding: 48px 16px; color: #999;">
    <p style="font-size: var(--rp-font-size-4xl); margin-bottom: 16px;">📝</p>
    <p>답변 데이터가 없습니다.</p>
  </div>
<% end %>

<script>
  function showFeedback(idx, type) {
    const modal = document.getElementById(`feedback-modal-${idx}-${type}`);
    if (modal) {
      modal.style.display = 'block';
    }
  }

  function closeFeedback(idx, type) {
    const modal = document.getElementById(`feedback-modal-${idx}-${type}`);
    if (modal) {
      modal.style.display = 'none';
    }
  }

  // 모달 외부 클릭 시 닫기
  window.onclick = function(event) {
    if (event.target.classList.contains('feedback-modal')) {
      event.target.style.display = 'none';
    }
  }
</script>

<% content_for :portal_actions do %>
  <a class="portal-button" href="<%= parent_reports_path(student_id: @student.id) %>">리포트 목록</a>
<% end %>
