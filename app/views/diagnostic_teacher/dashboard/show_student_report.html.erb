<% content_for :portal_title do %>
  진단 결과 상세
<% end %>

<% content_for :portal_subtitle do %>
  학생별 진단 결과 및 종합 보고서를 확인합니다.
<% end %>

<% content_for :portal_nav do %>
  <%= render "diagnostic_teacher/shared/nav", current: :reports %>
<% end %>

<style>
  .student-info-header {
    padding: 24px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border-radius: 8px;
    margin-bottom: 24px;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .student-info-left {
    flex: 1;
  }

  .student-name {
    font-size: 24px;
    font-weight: 700;
    margin-bottom: 8px;
  }

  .student-meta {
    font-size: 13px;
    opacity: 0.9;
    display: flex;
    gap: 16px;
  }

  .nav-buttons {
    display: flex;
    gap: 12px;
  }

  .nav-btn {
    padding: 12px 16px;
    background: rgba(255, 255, 255, 0.2);
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 4px;
    cursor: pointer;
    font-weight: 600;
    font-size: 14px;
    transition: all 0.3s ease;
    text-decoration: none;
    display: inline-block;
  }

  .nav-btn:hover:not(:disabled) {
    background: rgba(255, 255, 255, 0.3);
  }

  .nav-btn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  .content-wrapper {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    margin-bottom: 24px;
  }

  .panel {
    background: white;
    border: 1px solid #e0e0e0;
    border-radius: 8px;
    padding: 24px;
    overflow-y: auto;
    max-height: 800px;
  }

  .panel-title {
    font-size: 16px;
    font-weight: 600;
    color: #333;
    margin-bottom: 16px;
    padding-bottom: 12px;
    border-bottom: 2px solid #667eea;
  }

  /* 결과 보기 스타일 */
  .section-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin: 24px 0 16px 0;
  }

  .section-title {
    font-size: 14px;
    font-weight: 600;
    color: #333;
  }

  .section-count {
    background: #667eea;
    color: white;
    padding: 2px 8px;
    border-radius: 12px;
    font-size: 11px;
    font-weight: 600;
  }

  .result-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 12px;
    margin-bottom: 16px;
  }

  .result-table thead {
    background: #f8f9fa;
  }

  .result-table th {
    padding: 8px;
    text-align: left;
    font-weight: 600;
    font-size: 11px;
    color: #333;
    border-bottom: 1px solid #ddd;
  }

  .result-table td {
    padding: 8px;
    border-bottom: 1px solid #eee;
    font-size: 11px;
  }

  .result-table tbody tr:hover {
    background: #f8f9fa;
  }

  .cell-number {
    font-weight: 600;
    color: #667eea;
    width: 40px;
  }

  .correct-badge {
    display: inline-block;
    background: #d4edda;
    color: #155724;
    padding: 2px 6px;
    border-radius: 3px;
    font-size: 10px;
    font-weight: 600;
  }

  .incorrect-badge {
    display: inline-block;
    background: #f8d7da;
    color: #721c24;
    padding: 2px 6px;
    border-radius: 3px;
    font-size: 10px;
    font-weight: 600;
  }

  /* 종합 보고서 스타일 */
  .insight-box {
    background: #f8f9fa;
    border-left: 4px solid #667eea;
    padding: 12px;
    margin-bottom: 12px;
    border-radius: 4px;
    font-size: 12px;
  }

  .insight-label {
    font-size: 10px;
    color: #999;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 4px;
  }

  .insight-value {
    font-size: 13px;
    font-weight: 600;
    color: #333;
    line-height: 1.5;
  }

  .achievement-card {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 12px;
    border-radius: 6px;
    text-align: center;
    margin-bottom: 12px;
  }

  .achievement-card h4 {
    margin: 0 0 6px 0;
    font-size: 12px;
    opacity: 0.9;
  }

  .achievement-score {
    font-size: 18px;
    font-weight: 700;
  }

  .achievement-rate {
    font-size: 11px;
    opacity: 0.8;
  }

  .reader-type-box {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 16px;
    border-radius: 6px;
    text-align: center;
    margin-bottom: 16px;
  }

  .reader-type-box h3 {
    margin: 0 0 6px 0;
    font-size: 12px;
    opacity: 0.9;
  }

  .reader-type-name {
    font-size: 16px;
    font-weight: 700;
    margin: 8px 0;
  }

  .reader-type-desc {
    font-size: 11px;
    opacity: 0.8;
    line-height: 1.4;
  }

  .direction-item {
    padding: 12px;
    background: #f8f9fa;
    border-radius: 4px;
    margin-bottom: 12px;
    border-left: 4px solid #667eea;
    font-size: 12px;
  }

  .direction-item h4 {
    margin: 0 0 6px 0;
    color: #333;
    font-size: 12px;
  }

  .direction-item p {
    margin: 0;
    color: #666;
    font-size: 11px;
    line-height: 1.4;
  }

  .back-link {
    display: inline-block;
    margin-bottom: 16px;
    color: #667eea;
    text-decoration: none;
    font-size: 14px;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  @media (max-width: 1200px) {
    .content-wrapper {
      grid-template-columns: 1fr;
    }

    .panel {
      max-height: none;
    }
  }
</style>

<a href="<%= diagnostic_teacher_reports_path %>" class="back-link">← 목록으로 돌아가기</a>

<!-- 학생 정보 헤더 -->
<div class="student-info-header">
  <div class="student-info-left">
    <div class="student-name"><%= @student.name %></div>
    <div class="student-meta">
      <span>ID: <%= @student.id %></span>
      <span>진단 ID: #<%= @attempt.id %></span>
      <span>진단일: <%= @attempt.started_at&.strftime("%Y.%m.%d %H:%M") %></span>
    </div>
  </div>

  <div class="nav-buttons">
    <% if @prev_student_id %>
      <% prev_student = Student.find(@prev_student_id) %>
      <% prev_attempt = prev_student.student_attempts.order(created_at: :desc).first %>
      <% if prev_attempt %>
        <a href="<%= diagnostic_teacher_show_student_report_path(@prev_student_id, attempt_id: prev_attempt.id) %>" class="nav-btn">
          ← 이전
        </a>
      <% else %>
        <button class="nav-btn" disabled>← 이전</button>
      <% end %>
    <% else %>
      <button class="nav-btn" disabled>← 이전</button>
    <% end %>

    <% if @next_student_id %>
      <% next_student = Student.find(@next_student_id) %>
      <% next_attempt = next_student.student_attempts.order(created_at: :desc).first %>
      <% if next_attempt %>
        <a href="<%= diagnostic_teacher_show_student_report_path(@next_student_id, attempt_id: next_attempt.id) %>" class="nav-btn">
          다음 →
        </a>
      <% else %>
        <button class="nav-btn" disabled>다음 →</button>
      <% end %>
    <% else %>
      <button class="nav-btn" disabled>다음 →</button>
    <% end %>
  </div>
</div>

<!-- 좌우 분할 컨텐츠 -->
<div class="content-wrapper">
  <!-- 왼쪽: 결과 보기 -->
  <div class="panel">
    <div class="panel-title">📋 진단 결과 상세</div>

    <% if @mcq_responses.any? %>
      <div class="section-header">
        <span class="section-title">객관식 문항</span>
        <span class="section-count"><%= @mcq_responses.count %>문항</span>
      </div>

      <table class="result-table">
        <thead>
          <tr>
            <th style="width: 30px;">번호</th>
            <th style="width: 100px;">평가지표</th>
            <th style="width: 50px;">정오</th>
          </tr>
        </thead>
        <tbody>
          <% @mcq_responses.each_with_index do |response, idx| %>
            <% is_correct = response.selected_choice&.is_correct %>
            <tr>
              <td class="cell-number"><%= idx + 1 %></td>
              <td><%= response.item.evaluation_indicator&.name %></td>
              <td>
                <% if is_correct %>
                  <span class="correct-badge">정답</span>
                <% else %>
                  <span class="incorrect-badge">오답</span>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% end %>

    <% if @constructed_responses.any? %>
      <div class="section-header">
        <span class="section-title">서술형 문항</span>
        <span class="section-count"><%= @constructed_responses.count %>문항</span>
      </div>

      <table class="result-table">
        <thead>
          <tr>
            <th style="width: 30px;">번호</th>
            <th style="width: 100px;">평가지표</th>
            <th style="width: 50px;">평가점</th>
          </tr>
        </thead>
        <tbody>
          <% @constructed_responses.each_with_index do |response, idx| %>
            <tr>
              <td class="cell-number"><%= @mcq_responses.count + idx + 1 %></td>
              <td><%= response.item.evaluation_indicator&.name %></td>
              <td>
                <% if response.response_rubric_scores.any? %>
                  <% total_score = response.response_rubric_scores.sum(&:level_score) %>
                  <%= total_score %>점
                <% else %>
                  평가중
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% end %>

    <% if @responses.empty? %>
      <div style="text-align: center; padding: 32px 16px; color: #999;">
        <p>답변 데이터가 없습니다.</p>
      </div>
    <% end %>
  </div>

  <!-- 오른쪽: 종합 보고서 -->
  <div class="panel">
    <div class="panel-title">📊 종합 보고서</div>

    <!-- 종합 분석 (AttemptReport 데이터) -->
    <% if @report %>
      <% if @report.comprehensive_report_generated? %>
        <div class="insight-box">
          <div class="insight-label">종합 평가</div>
          <div class="insight-value">
            <%= @report.section_content('overall_summary').presence || @report.strengths || "평가 중입니다." %>
          </div>
        </div>

        <% if @report.section_content('comprehensive_opinion').present? || @report.weaknesses.present? %>
          <div class="insight-box">
            <div class="insight-label">종합 의견</div>
            <div class="insight-value">
              <%= @report.section_content('comprehensive_opinion').presence || @report.weaknesses %>
            </div>
          </div>
        <% end %>

        <% if @report.section_content('learning_recommendations').present? || @report.recommendations.present? %>
          <div class="insight-box">
            <div class="insight-label">학습 권고사항</div>
            <div class="insight-value">
              <%= @report.section_content('learning_recommendations').presence || @report.recommendations %>
            </div>
          </div>
        <% end %>
      <% else %>
        <% if @report.strengths.present? %>
          <div class="insight-box">
            <div class="insight-label">강점</div>
            <div class="insight-value"><%= @report.strengths %></div>
          </div>
        <% end %>

        <% if @report.weaknesses.present? %>
          <div class="insight-box">
            <div class="insight-label">개선 영역</div>
            <div class="insight-value"><%= @report.weaknesses %></div>
          </div>
        <% end %>

        <% if @report.recommendations.present? %>
          <div class="insight-box">
            <div class="insight-label">권고사항</div>
            <div class="insight-value"><%= @report.recommendations %></div>
          </div>
        <% end %>
      <% end %>
    <% end %>

    <!-- 독자 성향 -->
    <% if @reader_tendency %>
      <div class="reader-type-box">
        <h3>독자 성향</h3>
        <div class="reader-type-name">
          <%= @reader_tendency.reading_speed&.capitalize || "분석중" %> (<%= @reader_tendency.words_per_minute || 0 %> WPM)
        </div>
        <div class="reader-type-desc">
          <%= @reader_tendency.tendency_summary || "읽기 성향을 분석합니다." %>
        </div>
      </div>
    <% end %>

    <!-- 문해력 성취 -->
    <% if @literacy_achievements.any? %>
      <div style="margin-bottom: 16px;">
        <% @literacy_achievements.each do |achievement| %>
          <div class="achievement-card">
            <h4><%= achievement.evaluation_indicator&.name || "문해력" %></h4>
            <div class="achievement-score"><%= achievement.correct_answers %>/<%= achievement.total_questions %></div>
            <div class="achievement-rate">정답률: <%= achievement.accuracy_rate %>%</div>
          </div>
        <% end %>
      </div>
    <% end %>

    <!-- 학습 지도 방향 -->
    <% if @guidance_directions.any? %>
      <% @guidance_directions.each do |direction| %>
        <div class="direction-item">
          <h4><%= direction.evaluation_indicator&.name || "학습 영역" %></h4>
          <p><%= direction.content || "지도 내용을 준비 중입니다." %></p>
        </div>
      <% end %>
    <% end %>

    <% unless @report || @reader_tendency || @literacy_achievements.any? || @guidance_directions.any? %>
      <div style="text-align: center; padding: 32px 16px; color: #999;">
        <p style="font-size: 12px;">분석 데이터가 아직 준비 중입니다.</p>
      </div>
    <% end %>
  </div>
</div>

<% content_for :portal_actions do %>
  <a class="portal-button" href="<%= diagnostic_teacher_reports_path %>">목록으로 돌아가기</a>
<% end %>
