<% content_for :page_title, "MCQ 피드백 관리" %>

<div class="rp-page-header">
  <h1 class="rp-page-header__title">MCQ 피드백 관리</h1>
  <p class="rp-page-header__subtitle">객관식 문항에 대한 피드백을 제공하세요</p>
</div>

<!-- 통계 카드 -->
<div class="rp-stats-grid">
  <div class="rp-stat-card">
    <div class="rp-stat-card__label">피드백 필요 학생</div>
    <div class="rp-stat-card__value"><%= @students_count %></div>
  </div>
  <div class="rp-stat-card">
    <div class="rp-stat-card__label">총 피드백 필요 응답</div>
    <div class="rp-stat-card__value rp-stat-card__value--warning"><%= @responses_count %></div>
  </div>
</div>

<!-- 검색 및 필터 -->
<div class="rp-filter-section">
  <%= form_with url: diagnostic_teacher_feedbacks_path, method: :get, class: "rp-filter-form", local: true do |f| %>
    <div class="rp-filter-form__row">
      <!-- 검색 -->
      <div class="rp-filter-form__group rp-filter-form__group--search">
        <%= f.text_field :search,
            value: @search_query,
            placeholder: "학생 ID로 검색",
            class: "rp-input" %>
      </div>

      <div class="rp-filter-form__actions">
        <%= f.submit "검색", class: "rp-btn rp-btn--primary" %>
        <%= link_to "초기화", diagnostic_teacher_feedbacks_path, class: "rp-btn rp-btn--secondary" %>
      </div>
    </div>
  <% end %>
</div>

<!-- 학생 목록 -->
<div class="rp-feedback-list">
  <% if @student_responses.any? %>
    <div class="rp-table-responsive">
      <table class="rp-table">
        <thead>
          <tr>
            <th class="rp-table__th" style="width: 20%;">학생 ID</th>
            <th class="rp-table__th" style="width: 12%;">학년/반</th>
            <th class="rp-table__th" style="width: 15%;">학교</th>
            <th class="rp-table__th" style="width: 15%;">피드백 필요 응답</th>
            <th class="rp-table__th" style="width: 15%;">최근 응답</th>
            <th class="rp-table__th" style="width: 13%;">상태</th>
            <th class="rp-table__th" style="width: 10%;"></th>
          </tr>
        </thead>
        <tbody>
          <% @student_responses.each do |student_id, responses| %>
            <% student = responses.first.attempt.student %>
            <% latest_attempt = responses.first.attempt %>
            <% feedback_count = responses.select { |r| r.response_feedbacks.empty? }.count %>
            <% is_published = latest_attempt.feedback_published_at.present? %>
            <tr class="rp-table__row">
              <td class="rp-table__td"><strong><%= student.name %></strong></td>
              <td class="rp-table__td"><%= "#{student.grade}학년 #{student.class_name}반" %></td>
              <td class="rp-table__td"><%= student.school.name %></td>
              <td class="rp-table__td">
                <span class="rp-feedback-count">
                  <%= feedback_count %> / <%= responses.count %>
                </span>
              </td>
              <td class="rp-table__td">
                <%= time_ago_in_words(responses.max_by { |r| r.created_at }.created_at) %> 전
              </td>
              <td class="rp-table__td">
                <% if is_published %>
                  <span class="rp-badge rp-badge--published">피드백 완료</span>
                <% elsif feedback_count > 0 %>
                  <span class="rp-badge rp-badge--warning">피드백필요</span>
                <% else %>
                  <span class="rp-badge rp-badge--success">완료</span>
                <% end %>
              </td>
              <td class="rp-table__td">
                <%= link_to "보기", diagnostic_teacher_feedback_path(student.id), class: "rp-btn rp-btn--small rp-btn--primary" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <!-- 페이지네이션 -->
    <div class="rp-pagination">
      <%= paginate @student_responses, param_name: :page %>
    </div>
  <% else %>
    <div class="rp-empty-state">
      <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2z"></path>
        <path d="M12 6v6l4.25 2.52"></path>
      </svg>
      <h3>피드백이 필요한 학생이 없습니다</h3>
      <p>모든 MCQ 응답에 피드백이 제공되었습니다</p>
    </div>
  <% end %>
</div>

<style>
  .rp-stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 16px;
    margin-bottom: 24px;
  }

  .rp-stat-card {
    background: white;
    border: 1px solid #e2e8f0;
    border-radius: 12px;
    padding: 16px;
    text-align: center;
  }

  .rp-stat-card__label {
    font-size: 13px;
    color: #94a3b8;
    margin-bottom: 8px;
  }

  .rp-stat-card__value {
    font-size: 28px;
    font-weight: 700;
    color: #0f172a;
  }

  .rp-stat-card__value--warning {
    color: #f59e0b;
  }

  .rp-page-header {
    margin-bottom: 24px;
  }

  .rp-page-header__title {
    font-size: 28px;
    font-weight: 700;
    color: #0f172a;
    margin: 0 0 8px 0;
  }

  .rp-page-header__subtitle {
    font-size: 14px;
    color: #94a3b8;
    margin: 0;
  }

  .rp-filter-section {
    background: white;
    border: 1px solid #e2e8f0;
    border-radius: 12px;
    padding: 16px;
    margin-bottom: 24px;
  }

  .rp-filter-form__row {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 12px;
    align-items: flex-end;
  }

  .rp-filter-form__group--search {
    grid-column: 1;
  }

  .rp-filter-form__actions {
    grid-column: 2;
    display: flex;
    gap: 8px;
  }

  .rp-table-responsive {
    overflow-x: auto;
    border-radius: 12px;
    border: 1px solid #e2e8f0;
  }

  .rp-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 14px;
  }

  .rp-table th,
  .rp-table td {
    padding: 12px 16px;
    text-align: left;
    border-bottom: 1px solid #e2e8f0;
  }

  .rp-table th {
    font-weight: 600;
    color: #94a3b8;
    background: #f8fafc;
  }

  .rp-feedback-count {
    font-weight: 600;
    color: #0f172a;
  }

  .rp-badge {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    padding: 4px 8px;
    border-radius: 6px;
    font-size: 12px;
    font-weight: 600;
  }

  .rp-badge--success {
    background: #dcfce7;
    color: #15803d;
  }

  .rp-badge--warning {
    background: #fef3c7;
    color: #b45309;
  }

  .rp-badge--published {
    background: #dbeafe;
    color: #1e40af;
  }

  .rp-btn {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 12px;
    border-radius: 6px;
    font-size: 12px;
    font-weight: 500;
    text-decoration: none;
    cursor: pointer;
    border: 1px solid transparent;
    transition: all 0.15s;
    white-space: nowrap;
  }

  .rp-btn--primary {
    background: #2f5bff;
    color: white;
  }

  .rp-btn--primary:hover {
    background: #1e4ae8;
  }

  .rp-btn--secondary {
    background: #ffffff;
    color: #475569;
    border-color: #e2e8f0;
  }

  .rp-btn--secondary:hover {
    background: #f8fafc;
    border-color: #cbd5e1;
  }

  .rp-btn--small {
    padding: 6px 12px;
    font-size: 12px;
  }

  .rp-empty-state {
    text-align: center;
    padding: 48px 24px;
    background: white;
    border-radius: 12px;
  }

  .rp-empty-state svg {
    color: #94a3b8;
    margin-bottom: 16px;
  }

  .rp-empty-state h3 {
    font-size: 18px;
    font-weight: 600;
    color: #0f172a;
    margin: 16px 0 8px 0;
  }

  .rp-empty-state p {
    font-size: 14px;
    color: #94a3b8;
    margin: 0;
  }

  .rp-pagination {
    display: flex;
    justify-content: center;
    gap: 8px;
    margin-top: 24px;
    padding: 16px;
  }

  .rp-input {
    padding: 8px 12px;
    border: 1px solid #e2e8f0;
    border-radius: 6px;
    font-size: 13px;
    width: 100%;
  }
</style>
