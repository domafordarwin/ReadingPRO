<% content_for :portal_title do %>
  문항 은행
<% end %>
<% content_for :portal_subtitle do %>
  완성된 문항(지문이 연결된 문항)을 확인하세요.
<% end %>
<% content_for :portal_nav do %>
  <%= render "researcher/shared/nav", current: :item_bank %>
<% end %>

<style>
  .item-bank-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 32px;
  }

  .item-bank-actions {
    display: flex;
    gap: 12px;
  }

  .btn-manage {
    padding: 10px 24px;
    background: #667eea;
    color: white;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    font-weight: 600;
    text-decoration: none;
    display: inline-block;
  }

  .btn-manage:hover {
    background: #764ba2;
  }

  .item-bank-stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
    margin-bottom: 32px;
  }

  .stat-card {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    padding: 24px;
    border-radius: 12px;
    color: white;
  }

  .stat-card:nth-child(2) {
    background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
  }

  .stat-card:nth-child(3) {
    background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
  }

  .stat-card:nth-child(4) {
    background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
  }

  .stat-card__label {
    font-size: 13px;
    opacity: 0.9;
    margin-bottom: 8px;
  }

  .stat-card__value {
    font-size: 32px;
    font-weight: 700;
  }

  .search-box {
    margin-bottom: 24px;
  }

  .search-box input {
    width: 100%;
    padding: 14px 20px;
    border: 2px solid #e0e0e0;
    border-radius: 8px;
    font-size: 15px;
    transition: all 0.2s;
  }

  .search-box input:focus {
    outline: none;
    border-color: #667eea;
    box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
  }

  .items-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    gap: 20px;
  }

  .item-card {
    background: white;
    border: 2px solid #e0e0e0;
    border-radius: 12px;
    padding: 20px;
    transition: all 0.3s ease;
    cursor: pointer;
  }

  .item-card:hover {
    border-color: #667eea;
    box-shadow: 0 8px 20px rgba(102, 126, 234, 0.15);
    transform: translateY(-2px);
  }

  .item-card__header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 16px;
  }

  .item-card__code {
    font-size: 14px;
    font-weight: 700;
    color: #667eea;
    background: #f0f4ff;
    padding: 6px 12px;
    border-radius: 6px;
  }

  .item-card__badges {
    display: flex;
    gap: 6px;
  }

  .badge {
    padding: 4px 10px;
    border-radius: 12px;
    font-size: 11px;
    font-weight: 600;
  }

  .badge-mcq {
    background: #e3f2fd;
    color: #1565c0;
  }

  .badge-constructed {
    background: #f3e5f5;
    color: #6a1b9a;
  }

  .badge-easy {
    background: #e8f5e9;
    color: #2e7d32;
  }

  .badge-medium {
    background: #fff3e0;
    color: #ef6c00;
  }

  .badge-hard {
    background: #ffebee;
    color: #c62828;
  }

  .item-card__prompt {
    font-size: 14px;
    color: #333;
    line-height: 1.6;
    margin-bottom: 16px;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .item-card__footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 16px;
    border-top: 1px solid #f0f0f0;
  }

  .item-card__stimulus {
    font-size: 12px;
    color: #666;
  }

  .item-card__stimulus-title {
    font-weight: 600;
    color: #333;
  }

  .item-card__action {
    padding: 6px 16px;
    background: #667eea;
    color: white;
    border: none;
    border-radius: 6px;
    font-size: 12px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.2s;
  }

  .item-card__action:hover {
    background: #764ba2;
  }

  .empty-state {
    text-align: center;
    padding: 80px 20px;
    color: #999;
  }

  .empty-state__icon {
    font-size: 64px;
    margin-bottom: 20px;
  }

  .empty-state__message {
    font-size: 18px;
    font-weight: 600;
    color: #666;
    margin-bottom: 8px;
  }

  .empty-state__submessage {
    font-size: 14px;
    color: #999;
  }

  @media (max-width: 1200px) {
    .item-bank-stats {
      grid-template-columns: repeat(2, 1fr);
    }

    .items-grid {
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    }
  }

  @media (max-width: 768px) {
    .item-bank-stats {
      grid-template-columns: 1fr;
    }

    .items-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<div class="portal-card">
  <!-- Flash Messages -->
  <% if flash[:notice] || flash[:alert] %>
    <div style="margin-bottom: 20px;">
      <% if flash[:notice] %>
        <div style="padding: 16px 20px; background: #d4edda; border: 1px solid #c3e6cb; border-radius: 8px; color: #155724; margin-bottom: 12px;">
          <strong>✅ 성공:</strong> <%= flash[:notice] %>
        </div>
      <% end %>
      <% if flash[:alert] %>
        <div style="padding: 16px 20px; background: #f8d7da; border: 1px solid #f5c6cb; border-radius: 8px; color: #721c24; margin-bottom: 12px;">
          <strong>⚠️ 오류:</strong> <%= flash[:alert] %>
        </div>
      <% end %>
    </div>
  <% end %>

  <!-- 헤더 -->
  <div class="item-bank-header">
    <h2 style="margin: 0; font-size: 20px; font-weight: 700; color: #333;">완성된 문항 목록</h2>
    <div class="item-bank-actions">
      <button type="button" class="btn-manage" style="background: #28a745;" onclick="document.getElementById('pdf-upload-input').click()">
        📄 완성된 문항 등록 (PDF)
      </button>
      <%= link_to "문항 관리로 이동", researcher_items_path, class: "btn-manage" %>
    </div>
  </div>

  <!-- Hidden PDF Upload Form -->
  <form id="pdf-upload-form" action="<%= researcher_upload_pdf_item_bank_path %>" method="post" enctype="multipart/form-data" style="display: none;">
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <input type="file" id="pdf-upload-input" name="pdf_file" accept=".pdf" onchange="this.form.submit()">
  </form>

  <!-- 통계 카드 -->
  <div class="item-bank-stats">
    <div class="stat-card">
      <div class="stat-card__label">총 완성 문항</div>
      <div class="stat-card__value"><%= @total_count %></div>
    </div>
    <div class="stat-card">
      <div class="stat-card__label">객관식 문항</div>
      <div class="stat-card__value"><%= @items.count { |i| i.item_type == 'mcq' } %></div>
    </div>
    <div class="stat-card">
      <div class="stat-card__label">주관식 문항</div>
      <div class="stat-card__value"><%= @items.count { |i| i.item_type == 'constructed' } %></div>
    </div>
    <div class="stat-card">
      <div class="stat-card__label">활성 문항</div>
      <div class="stat-card__value"><%= @items.count { |i| i.status == 'active' } %></div>
    </div>
  </div>

  <!-- 간단 검색 -->
  <div class="search-box">
    <%= form_with url: researcher_item_bank_path, method: :get, local: true do |f| %>
      <%= f.text_field :search,
          placeholder: "문항 코드나 내용으로 검색...",
          value: @search_query,
          onkeyup: "this.form.requestSubmit()" %>
    <% end %>
  </div>

  <!-- 문항 카드 그리드 -->
  <% if @items.any? %>
    <div class="items-grid">
      <% @items.each do |item| %>
        <div class="item-card" onclick="window.location.href='<%= edit_researcher_item_path(item) %>'">
          <div class="item-card__header">
            <div class="item-card__code"><%= item.code || "##{item.id}" %></div>
            <div class="item-card__badges">
              <span class="badge <%= item.item_type == 'mcq' ? 'badge-mcq' : 'badge-constructed' %>">
                <%= item.item_type == 'mcq' ? '객관식' : '주관식' %>
              </span>
              <% if item.difficulty.present? %>
                <span class="badge badge-<%= item.difficulty %>">
                  <%= item.difficulty == 'easy' ? '하' : (item.difficulty == 'medium' ? '중' : '상') %>
                </span>
              <% end %>
            </div>
          </div>

          <div class="item-card__prompt">
            <%= item.prompt %>
          </div>

          <div class="item-card__footer">
            <div class="item-card__stimulus">
              <div style="margin-bottom: 4px; font-size: 11px; text-transform: uppercase; letter-spacing: 0.5px;">지문</div>
              <div class="item-card__stimulus-title">
                <%= truncate(item.stimulus&.title || '지문 없음', length: 30) %>
              </div>
            </div>
            <%= link_to "편집", edit_researcher_item_path(item), class: "item-card__action", onclick: "event.stopPropagation()" %>
          </div>
        </div>
      <% end %>
    </div>

    <!-- 페이지네이션 -->
    <% if @has_next || @has_prev %>
      <div style="display: flex; justify-content: center; gap: 12px; margin-top: 32px;">
        <% if @has_prev %>
          <%= link_to "← 이전", researcher_item_bank_path(cursor: @prev_cursor, direction: "backward", search: @search_query),
              class: "btn-manage" %>
        <% else %>
          <span style="padding: 10px 24px; background: #f0f0f0; color: #ccc; border-radius: 6px; font-weight: 600;">← 이전</span>
        <% end %>

        <% if @has_next %>
          <%= link_to "다음 →", researcher_item_bank_path(cursor: @next_cursor, direction: "forward", search: @search_query),
              class: "btn-manage" %>
        <% else %>
          <span style="padding: 10px 24px; background: #f0f0f0; color: #ccc; border-radius: 6px; font-weight: 600;">다음 →</span>
        <% end %>
      </div>
    <% end %>
  <% else %>
    <div class="empty-state">
      <div class="empty-state__icon">📦</div>
      <div class="empty-state__message">완성된 문항이 없습니다</div>
      <div class="empty-state__submessage">
        <% if @search_query.present? %>
          "<%= @search_query %>"에 해당하는 문항이 없습니다.
        <% else %>
          지문과 연결된 완성 문항을 만들어보세요.
        <% end %>
      </div>
      <div style="margin-top: 24px;">
        <%= link_to "문항 관리로 이동", researcher_items_path, class: "btn-manage" %>
      </div>
    </div>
  <% end %>
</div>
