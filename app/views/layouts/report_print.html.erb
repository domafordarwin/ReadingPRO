<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title><%= yield :title %></title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: "Pretendard", -apple-system, BlinkMacSystemFont, sans-serif;
      font-size: 11px;
      line-height: 1.6;
      color: #1e293b;
      background: white;
      -webkit-print-color-adjust: exact;
      print-color-adjust: exact;
    }
    :root {
      /* 인쇄용 6단계 축소 체계 */
      --rp-font-size-xs:  10px;
      --rp-font-size-sm:  11px;
      --rp-font-size-md:  12px;
      --rp-font-size-lg:  14px;
      --rp-font-size-xl:  17px;
      --rp-font-size-2xl: 20px;
      --rp-primary: #667eea;
    }
    @page { size: A4; margin: 12mm 10mm; }
    p { margin: 0 0 0.5em; }
  </style>
  <%= yield :print_styles %>
</head>
<body>
  <%= yield %>
</body>
</html>
