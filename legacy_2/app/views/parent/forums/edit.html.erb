<% content_for :page_title do %>글 수정<% end %>

<!-- Page Header -->
<div class="rp-page-header">
  <h1 class="rp-page-header__title">글 수정</h1>
</div>

<!-- Forum Form -->
<div class="rp-card">
  <%= form_with model: @forum, local: true do |f| %>
    <% if @forum.errors.any? %>
      <div class="rp-flash rp-flash--alert">
        <strong><%= @forum.errors.count %>개의 오류가 있습니다:</strong>
        <ul style="margin: 8px 0; padding-left: 20px;">
          <% @forum.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="rp-form-group">
      <%= f.label :title, "제목" %>
      <%= f.text_field :title, class: "rp-input" %>
    </div>

    <div class="rp-form-group">
      <%= f.label :category, "카테고리" %>
      <%= f.select :category, ParentForum::CATEGORIES.map { |c| [ParentForum::CATEGORY_LABELS[c], c] }, {}, class: "rp-select" %>
    </div>

    <div class="rp-form-group">
      <%= f.label :content, "내용" %>
      <%= f.text_area :content, rows: 10, class: "rp-textarea" %>
    </div>

    <div class="rp-form-group" style="display: flex; gap: 12px; justify-content: flex-end;">
      <%= link_to "취소", parent_forum_path(@forum), class: "rp-btn rp-btn--secondary" %>
      <%= f.submit "수정 완료", class: "rp-btn rp-btn--primary" %>
    </div>
  <% end %>
</div>
